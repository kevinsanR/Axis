
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Notes</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    
    

    <div class="notes">
        <% notes.forEach(note => { %>
            <div class="note">
                <h2><%= note.title %></h2>
                <p><%= note.contents %></p>
                <p>Posted on: <%= new Date(note.timestamp).toLocaleDateString() %></p>
                <a href="/notes/<%= note.id %>">View More</a>
            </div>
        <% }) %>
    </div>

</body>
</html>
